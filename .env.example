JIRA_BASE_URL=https://framebridge.atlassian.net
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_BOARD_ID=
JIRA_PROJECT_KEY=KEEN

# --- Board-specific: only needed on a board other than KEEN ---
# Run `node src/check-config.js` after setting JIRA_BOARD_ID to see what your
# board actually uses. An unrecognised status is silently treated as unfinished.
# DONE_STATUSES=deployed,completed,done,closed,resolved,canceled,cancelled
# AWAITING_RELEASE_STATUSES=ready for theme deploy,awaiting release date
# BASELINE_SPRINT_NUM=22
